# Local development
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=your_database

# Cloud Run (set via Cloud Run environment variables)
# INSTANCE_CONNECTION_NAME=project:region:instance

# Deployment
GCP_PROJECT=your-project-id
GCP_REGION=us-central1
CLOUDSQL_INSTANCE=ayegee-db
